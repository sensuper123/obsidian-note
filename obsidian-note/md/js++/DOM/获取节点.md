# 获取节点的方式
```javascript
//document 在浏览器就是一整个HTML ，它是一个对象
//获取元素的方式
document.getElementById()
document.getElementsByClassName()
document.getElementsByTagName()
document.querySelector()
document.querySelectorAl()
//一般不使用querySelector，原因两个
1. 性能消耗比getElement高
2. 获得的数据是缓存的，如果获取元素后，再remove元素，获得的数据不会发生变化
```
# 节点
```javascript
节点包括 
1. 元素节点  ---  1
2. 属性节点  ---  2
3. 文本节点  ---  3
4. 注释节点  ---  8
5. documnet  ---  9
6. DocumentFragment  --- 11

遍历节点树，以下方法包括所有节点，如文本节点都会遍历出来
//获取父元素节点
var li = document.getElementByTagName('li')[0]
li.parentNode
//获取子元素节点
li.childNodes
//获取第一个子元素，最后一个子元素
li.firstChild li.lastChild
//获取兄弟元素   previousSibling
-------------------------------------
//遍历元素节点树
parentElement  //跟parentNode差不多，只是html.parentElement是null
children //只选出元素节点
childElementCount = children.length
firstElementChild lastElementChild
nextElementSibling  previousElementSibling

//获取属性节点
li.getAttributeNode('id')
```
# 节点属性
```javascript
//nodeName 获取节点姓名，如果是元素节点，就返回元素如div,返回div,文本节点返回#text,注册节点返回#comment
nodeName    只读、大写
//nodeValue 返回节点的值，如注释节点，返回注释内容，属性节点，返回值，文本节点返回文本，只有文本节点、属性节点、注释节点有值，其他都没值 
nodeValue   可写
//节点类型
nodeType 
```
# 封装childNodes
```javascript
 function elementNodes(node) {
      var temp = {
        length: 0,
        push: Array.prototype.push,
        slice: Array.prototype.slice,
      }
      //缓存node.childNodes长度
      var nodeLen = node.childNodes.length,
      //得到node.childNodes
        childNodes = node.childNodes
      for (var i = 0; i < nodeLen; i++) {
        var nodeItem = childNodes[i]
        if (nodeItem.nodeType === 1) {
          // temp[temp['length']] = nodeItem
          // temp.length++
          temp.push(nodeItem)
        }
      }
      return temp
    }
```